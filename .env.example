# Copia este archivo a .env (nunca subas .env al repositorio) y rellena
# tu propia clave para poder ejecutar en local:
#   node --env-file=.env scripts/fetch-market-news.mjs
#
# Consíguela gratis en https://finnhub.io/register

FINNHUB_API_KEY=
